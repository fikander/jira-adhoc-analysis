version: '2'
services:
  server:
    image: fikander/jiraadhocanalysis_server
    restart: always
    ports:
      - "8888:8888"
    volumes:
      - $PWD/notebooks:/notebooks
    env_file:
      - .env
